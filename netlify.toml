[build]
  command = "npm run build:web"
  publish = "dist-web"

# SPA routing - all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# API proxy
[[redirects]]
  from = "/api/*"
  to = "https://api.gathersync.app/api/:splat"
  status = 200
  force = true
